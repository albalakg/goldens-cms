const appState = {
    namespaced: true,

    state: {
        name: 'Goldens',
    },

    getters: {
        appName: state => state.name,
    },

    mutations: {
        
    },

    actions: {
        
    }
};

export default appState;